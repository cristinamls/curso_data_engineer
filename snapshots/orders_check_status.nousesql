{% snapshot orders_snapshot_check %}

    {{
        config(
          target_schema='snapshots',
          strategy='check',
          unique_key='id',
          check_cols=['status', 'preparing'],
        )
    }}

    select * from {{ source('sql_server_dbo', 'orders') }}

{% endsnapshot %}